import React from 'react';

export const metadata = {
  title: 'Generate bot',
};

export default function GenerateBotLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
